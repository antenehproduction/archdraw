name = "adi-proxy"
main = "proxy.js"
compatibility_date = "2024-11-01"

# Uncomment when ready to bind a custom domain:
# routes = [
#   { pattern = "proxy.archdraw.example/*", zone_name = "archdraw.example" }
# ]

# Observability (free tier):
[observability]
enabled = true
